using UnityEngine;
using TMPro;

public class MenuButton : MonoBehaviour
{
    private TMP_Text m_buttonText;

    void Awake()
    {
        //m_buttonText = this.GetComponentInChildren<TextMeshProUGUI>();
    }
    
    void Start()
    {
        //m_buttonText.color = Color.red;
    }
}
